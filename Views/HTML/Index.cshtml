@model ConversionViewModel

@await Component.InvokeAsync("ConversionForm", Model)

@section Scripts {
    <script>
        document.addEventListener("DOMContentLoaded", () => {
            hideContainer("create-full-document");
            hideContainer("use-small-palette");
            hideContainer("operating-system");
        });
    </script>
}
