@model ConversionViewModel

@await Component.InvokeAsync("ConversionForm", new { model = Model })

@section Scripts {
    <script>
        document.addEventListener("DOMContentLoaded", () => {
            hideContainer("font");
            hideContainer("create-full-document");
            hideContainer("background");
            hideContainer("characters");
        });
    </script>
}
